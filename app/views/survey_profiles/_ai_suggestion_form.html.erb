<%= turbo_frame_tag "ai_suggestion_form" do %>
  <div id="ai-suggestion-data" 
       data-job-id="<%= job_id %>"
       data-goal-title="<%= goal_title %>" 
       data-goal-description="<%= goal_description %>" 
       data-action-plan="<%= action_plan %>" 
       data-remaining-count="<%= remaining_count %>">
  </div>
<% end %>